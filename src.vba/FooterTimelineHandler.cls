' !!! Auto-generated by dump_vba.py !!!
Public WithEvents oApp As Application

Private Sub oApp_PresentationNewSlide(ByVal oSlide As slide)
    If oApp Is Nothing Then Exit Sub
    If oApp.ActivePresentation Is Nothing Then Exit Sub
    If HasMinSectionCount(oApp.ActivePresentation.SectionProperties) Then
        UpdateFooterOnSlide oSlide, GetFooterText(), GetFirstSectionIndex()
    End If
End Sub


Private Sub oApp_SlideSelectionChanged(ByVal SldRange As SlideRange)
    If oApp Is Nothing Then Exit Sub
    If oApp.ActivePresentation Is Nothing Then Exit Sub
    If isAutoUpdateSectionTimeline Then
        SlideOrderChanged
    End If
'    If Not ribbonUI Is Nothing Then ribbonUI.Invalidate
End Sub


'Private Sub oApp_WindowActivate(ByVal Pres As Presentation, ByVal Wn As DocumentWindow)
'    If Not ribbonUI Is Nothing Then ribbonUI.Invalidate
'End Sub


Private Sub App_ProtectedViewWindowBeforeEdit(ByVal PvWindow As ProtectedViewWindow, Cancel As Boolean)
 Dim intResponse As Integer
 
 intResponse = MsgBox("Do you really " _
 & "want to edit the document?", _
 vbYesNo)
 
 If intResponse = vbNo Then Cancel = True
End Sub